spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    username: root
    password: marchese1593
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create-drop
